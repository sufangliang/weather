# 天气
根据城市查询当前天气以及未来7天天气。iPhone，SwiftUI，iOS 17及以上。

## 运行
使用 Xcode 打开 `Tianqi.xcodeproj`，选择一台 iPhone 模拟器，点击运行。需要网络。

命令行：
```bash
xcodebuild -scheme Tianqi -destination 'platform=iOS Simulator,name=iPhone 17' build
```
无第三方依赖库，也不需要 API Key。

